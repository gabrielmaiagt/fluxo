'use client';

/**
 * @fileoverview Este arquivo foi intencionalmente deixado em branco.
 * A lógica de notificações push via Firebase Cloud Messaging (FCM) e VAPID foi desativada
 * a pedido do usuário para utilizar apenas a API de Notificações nativa do navegador,
 * que é gerenciada diretamente pelos componentes da interface.
 */
